
import { View, Text, ScrollView} from "react-native";

export default function Loginstore(){
    return (
        <ScrollView className="mt-8">
            <Text>login store Screen</Text>
        </ScrollView>
    )
}