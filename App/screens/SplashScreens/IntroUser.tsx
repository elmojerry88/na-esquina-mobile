
import { View, Text, ScrollView} from "react-native";

export default function IntroUser(){
    return (
        <ScrollView className="mt-8">
            <Text>INTRO USER SCREEN</Text>
        </ScrollView>
    )
}