import { View, Text, ScrollView} from "react-native";

export default function IntroStore(){
    return (
        <ScrollView className="mt-8">
            <Text>INTRO STORE SCREEN</Text>
        </ScrollView>
    )
}